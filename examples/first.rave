import <std/io>
import <std/threads>

int a(void* b) {
    std::printf("Hello, world!\n");
    std::threads::exit();
    std::printf("Hello, world!\n");
} => 0;

int main {
    long thread;
    std::threads::create(&thread,a,null);
    std::threads::start(thread);
} => 0;