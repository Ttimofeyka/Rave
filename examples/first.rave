import <std/io>
import <std/string>

struct A {
    int a;
    int b;
}

int main {
    std::string* A = std::string("Hello, world!");
        A.replace('e','b');
        std::printf("A is: %s\n",A.ptr);
    delete(A);
    return 0;
}