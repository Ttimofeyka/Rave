import <std/io>
import <std/string>

int main {
    std::string A = std::string("Hello!\n");
        std::printf("%s",A.ptr);
        A.replace('H','D');
        std::printf("%s",A.ptr);
    A.delete();
    return 0;
}