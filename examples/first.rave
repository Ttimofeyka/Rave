import <std/io> <std/network>

void main {
    std::network::URL koboldcpp = std::network::URL("http://localhost");

    auto res = std::network::get(koboldcpp, "5001");
    std::println(res.success);
}