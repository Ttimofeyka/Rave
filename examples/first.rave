import <std/io>

int A(vararg args) {
    
} => cast(int)args.vars[1].value;

int main {
    vararg args;

    args.length = 1;
    args.vars[0].value = cast(void*)5;
    args.vars[1].value = cast(void*)7;
} => A(args);