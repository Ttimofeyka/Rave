import <std/io> <std/utf> <std/cstring>

void main {
    char* s = "а";
    std::ustring us = cast(uint*)null;
    uint c = std::utf::decode(2, s);
    us.add(c);
    std::println(std::cstring::strlen(s), " ", us);
}