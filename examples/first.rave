import <std/io> <std/vector>

void main {
    std::u8string s = "☣ a m a l";
    auto parts = s.split(' ');
    std::println(parts.length);
    for(int i=0; i<parts.length; i+=1) {
        std::println(parts.data[i]);
    }
}