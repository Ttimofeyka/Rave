import <std/io> <std/thread>

int jam(void* arg) {
    std::println("Hello, cross-platform, from thread-safe Rave!");
}

void main {
    std::thread thr = std::thread();
    thr.run(jam, null);
    thr.join();
}