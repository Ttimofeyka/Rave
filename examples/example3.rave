@inc "std/io";

main: int {
    a: char* = "Hello, world!";
    // Код с ошибкой
    // puts(a);
    // Этот тоже
    // write(1,a,strlen(a));
    // Но этот - без
    b: int = strlen(a);
    write(1, a, b);
    write(1, a, strlen(a))
    ret 0;
}
