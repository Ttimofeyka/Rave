import <std/io>

namespace libc {
    extern(linkname: "__errno_location") int* _errno();
    (inline) int errno => libc::_errno()[0];
}

namespace std {
    namespace error {
        void abortWith(char* msg) {
            std::printf(msg);
            std::exit(0);
        }
    }
}