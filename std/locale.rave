namespace std {
    namespace locale {
        alias CTYPE = 0;
        alias NUMERIC = 1;
        alias TIME = 2;
        alias COLLATE = 3;
        alias MONETARY = 4;
        alias MESSAGES = 5;
        alias ALL = 6;

        extern(linkname: "setlocale") char* set(int category, char* locale);
    }
}
