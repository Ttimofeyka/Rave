import <std/memory>

namespace std {
    struct vector<T> {
        T* data;
        int capacity;
        int current;

        std::vector<T> this {
            this.data = new(T[1]);
            this.capacity = 1;
            this.current = 0;
        } => this;
    }
}