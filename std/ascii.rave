namespace std {
    namespace ascii {
        extern(linkname: "isspace") bool isSpace(char c);
        extern(linkname: "islower") bool isLower(char c);
        extern(linkname: "isupper") bool isUpper(char c);
        extern(linkname: "tolower") char toLower(char c);
        extern(linkname: "toupper") char toUpper(char c);
        extern(linkname: "isdigit") char isDigit(char c);
        extern(linkname: "strchr") char charInChars(char* chars, char c);
    }
}
