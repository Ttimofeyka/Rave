@protected

@inc "std/str/strlen";

strcmp(one: char*, two: char*): bool {
    i := 0;
    if(strlen(one) != strlen(two)) ret false;
    while(one[i] != '\0') {
        if(one[i] != two[i]) ret false;
        i += 1;
    }
    ret true;
}

@end